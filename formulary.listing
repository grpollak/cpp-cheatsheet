            myfu(int num_args, ...){
                va_list l;
                va_start(l, num_args);
                for(int i = 0; i < num_args; i++)
                    |\optc{\texttt{do\_something}}|(va_arg(l, |\optc{\texttt{type}}|))
                va_end(l);
            }
        
